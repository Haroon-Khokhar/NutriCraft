import React from 'react';
import {Text} from 'react-native';
import {ScreenWrapper, CustomText} from '../../components';

const Otp = () => {
  return (
    <ScreenWrapper>
      <CustomText title={'OTP screen'} fontSize={18} style={{marginTop: 40}} />
    </ScreenWrapper>
  );
};

// const styles = StyleSheet.create({});

export default Otp;
